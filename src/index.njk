---
layout: base.njk
title: Mike's Second Brain
---
<h1>{{ title }}</h1>
<p class="sub">Senior Director, AI Programs · CodePath</p>

<section class="intro">
  <p>A public-facing view into how I use Claude Cowork and a structured knowledge system at CodePath.</p>
</section>

<section class="stats">
  <h2>Current state</h2>
  <dl>
    <dt>Day</dt><dd>{{ metrics.days_active }}</dd>
    <dt>Files tracked</dt><dd>{{ metrics.total_files }}</dd>
    <dt>Lines of context</dt><dd>{{ metrics.total_lines }}</dd>
    <dt>Total size</dt><dd>{{ metrics.total_kb }} KB</dd>
  </dl>
</section>

<section class="posts">
  <h2>Build log</h2>
  {% set posts = collections.post | reverse %}
  {% if posts.length == 0 %}
    <p class="note">No posts yet.</p>
  {% else %}
    <ul class="post-list">
      {%- for post in posts -%}
        {%- if loop.index0 < 5 %}
        <li>
          <a href="{{ post.url | url }}">{{ post.data.title }}</a>
          <span class="post-date"><time datetime="{{ post.date | date('iso') }}">{{ post.date | date }}</time></span>
          {% if post.data.summary %}<p class="post-summary">{{ post.data.summary }}</p>{% endif %}
        </li>
        {% endif -%}
      {%- endfor %}
    </ul>
  {% endif %}
</section>
